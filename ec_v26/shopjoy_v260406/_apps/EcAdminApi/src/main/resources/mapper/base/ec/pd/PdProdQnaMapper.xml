<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shopjoy.ecadminapi.base.ec.pd.mapper.PdProdQnaMapper">

    <!-- 상품 문의 조회 조건 -->
    <sql id="pdProdQnaCond">
        <where>
            <if test="p.siteId != null and p.siteId != ''">AND q.site_id = #{p.siteId}</if>
            <if test="p.typeCd != null and p.typeCd != ''">AND q.qna_type_cd = #{p.typeCd}</if>
            <if test="p.kw != null and p.kw != ''">
                AND (q.qna_title ILIKE '%' || #{p.kw} || '%')
            </if>
            <if test="p.dateStart != null and p.dateStart != ''">AND q.reg_date &gt;= #{p.dateStart}::DATE</if>
            <if test="p.dateEnd   != null and p.dateEnd   != ''">AND q.reg_date &lt;  (#{p.dateEnd}::DATE + INTERVAL '1 day')</if>
        </where>
    </sql>

    <!-- 상품 문의 상세 조회 -->
    <select id="selectById" resultType="com.shopjoy.ecadminapi.base.ec.pd.data.dto.PdProdQnaDto">
        SELECT
            q.*,
            ste.site_nm AS site_nm,
            prd.prod_nm AS prod_nm,
            mem.member_nm AS member_nm,
            ord.member_nm AS order_nm,
            usr.user_nm AS answ_user_nm,
            cd_pqt.code_label AS qna_type_cd_nm
        FROM pd_prod_qna q
            LEFT JOIN sy_site ste ON ste.site_id = q.site_id
            LEFT JOIN pd_prod prd ON prd.prod_id = q.prod_id
            LEFT JOIN mb_member mem ON mem.member_id = q.member_id
            LEFT JOIN od_order ord ON ord.order_id = q.order_id
            LEFT JOIN sy_user usr ON usr.user_id = q.answ_user_id
            LEFT JOIN sy_code cd_pqt ON cd_pqt.code_grp = 'PROD_QNA_TYPE' AND cd_pqt.code_value = q.qna_type_cd
        WHERE q.qna_id = #{id}
    </select>

    <!-- 상품 문의 목록 조회 -->
    <select id="selectList" resultType="com.shopjoy.ecadminapi.base.ec.pd.data.dto.PdProdQnaDto">
        SELECT
            q.*,
            ste.site_nm AS site_nm,
            prd.prod_nm AS prod_nm,
            mem.member_nm AS member_nm,
            ord.member_nm AS order_nm,
            usr.user_nm AS answ_user_nm,
            cd_pqt.code_label AS qna_type_cd_nm
        FROM pd_prod_qna q
            LEFT JOIN sy_site ste ON ste.site_id = q.site_id
            LEFT JOIN pd_prod prd ON prd.prod_id = q.prod_id
            LEFT JOIN mb_member mem ON mem.member_id = q.member_id
            LEFT JOIN od_order ord ON ord.order_id = q.order_id
            LEFT JOIN sy_user usr ON usr.user_id = q.answ_user_id
            LEFT JOIN sy_code cd_pqt ON cd_pqt.code_grp = 'PROD_QNA_TYPE' AND cd_pqt.code_value = q.qna_type_cd
            <include refid="pdProdQnaCond"/>
        <choose>
            <when test="p.sort == 'id_asc'">ORDER BY q.qna_id ASC</when>
            <when test="p.sort == 'id_desc'">ORDER BY q.qna_id DESC</when>
            <when test="p.sort == 'nm_asc'">ORDER BY q.qna_title ASC</when>
            <when test="p.sort == 'nm_desc'">ORDER BY q.qna_title DESC</when>
            <when test="p.sort == 'reg_asc'">ORDER BY q.reg_date ASC</when>
            <when test="p.sort == 'reg_desc'">ORDER BY q.reg_date DESC</when>
            <otherwise>ORDER BY q.reg_date DESC</otherwise>
        </choose>
        <if test="p.limit != null and p.offset != null">LIMIT #{p.limit} OFFSET #{p.offset}</if>
    </select>

    <!-- 상품 문의 페이징 목록 조회 -->
    <select id="selectPageList" resultType="com.shopjoy.ecadminapi.base.ec.pd.data.dto.PdProdQnaDto">
        SELECT
            q.*,
            ste.site_nm AS site_nm,
            prd.prod_nm AS prod_nm,
            mem.member_nm AS member_nm,
            ord.member_nm AS order_nm,
            usr.user_nm AS answ_user_nm,
            cd_pqt.code_label AS qna_type_cd_nm
        FROM pd_prod_qna q
            LEFT JOIN sy_site ste ON ste.site_id = q.site_id
            LEFT JOIN pd_prod prd ON prd.prod_id = q.prod_id
            LEFT JOIN mb_member mem ON mem.member_id = q.member_id
            LEFT JOIN od_order ord ON ord.order_id = q.order_id
            LEFT JOIN sy_user usr ON usr.user_id = q.answ_user_id
            LEFT JOIN sy_code cd_pqt ON cd_pqt.code_grp = 'PROD_QNA_TYPE' AND cd_pqt.code_value = q.qna_type_cd
            <include refid="pdProdQnaCond"/>
        <choose>
            <when test="p.sort == 'id_asc'">ORDER BY q.qna_id ASC</when>
            <when test="p.sort == 'id_desc'">ORDER BY q.qna_id DESC</when>
            <when test="p.sort == 'nm_asc'">ORDER BY q.qna_title ASC</when>
            <when test="p.sort == 'nm_desc'">ORDER BY q.qna_title DESC</when>
            <when test="p.sort == 'reg_asc'">ORDER BY q.reg_date ASC</when>
            <when test="p.sort == 'reg_desc'">ORDER BY q.reg_date DESC</when>
            <otherwise>ORDER BY q.reg_date DESC</otherwise>
        </choose>
        LIMIT #{p.limit} OFFSET #{p.offset}
    </select>

    <!-- 상품 문의 전체 건수 조회 -->
    <select id="selectPageCount" resultType="long">
        SELECT COUNT(*)
        FROM pd_prod_qna q
            <include refid="pdProdQnaCond"/>
    </select>

    <!-- 상품 문의 선택 필드 수정 -->
    <update id="updateSelective" parameterType="com.shopjoy.ecadminapi.base.ec.pd.data.entity.PdProdQna">
        UPDATE pd_prod_qna
        <set>
            <if test="siteId != null">site_id = #{siteId},</if>
            <if test="prodId != null">prod_id = #{prodId},</if>
            <if test="skuId != null">sku_id = #{skuId},</if>
            <if test="memberId != null">member_id = #{memberId},</if>
            <if test="orderId != null">order_id = #{orderId},</if>
            <if test="qnaTypeCd != null">qna_type_cd = #{qnaTypeCd},</if>
            <if test="qnaTitle != null">qna_title = #{qnaTitle},</if>
            <if test="qnaContent != null">qna_content = #{qnaContent},</if>
            <if test="scrtYn != null">scrt_yn = #{scrtYn},</if>
            <if test="answYn != null">answ_yn = #{answYn},</if>
            <if test="answContent != null">answ_content = #{answContent},</if>
            <if test="answDate != null">answ_date = #{answDate},</if>
            <if test="answUserId != null">answ_user_id = #{answUserId},</if>
            <if test="dispYn != null">disp_yn = #{dispYn},</if>
            <if test="useYn != null">use_yn = #{useYn},</if>
            <if test="updBy != null">upd_by = #{updBy},</if>
            <if test="updDate != null">upd_date = #{updDate},</if>
        </set>
        WHERE qna_id = #{qnaId}
    </update>

</mapper>
