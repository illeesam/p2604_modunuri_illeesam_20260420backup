<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shopjoy.ecadminapi.base.ec.pm.mapper.PmCacheMapper">

    <!-- 캐시(충전금) 조회 조건 -->
    <sql id="pmCacheCond">
        <where>
            <if test="p.siteId != null and p.siteId != ''">AND c.site_id = #{p.siteId}</if>
            <if test="p.typeCd != null and p.typeCd != ''">AND c.cache_type_cd = #{p.typeCd}</if>
            <if test="p.kw != null and p.kw != ''">
                AND (c.member_nm ILIKE '%' || #{p.kw} || '%')
            </if>
            <if test="p.dateStart != null and p.dateStart != ''">AND c.reg_date &gt;= #{p.dateStart}::DATE</if>
            <if test="p.dateEnd   != null and p.dateEnd   != ''">AND c.reg_date &lt;  (#{p.dateEnd}::DATE + INTERVAL '1 day')</if>
        </where>
    </sql>

    <!-- 캐시(충전금) 상세 조회 -->
    <select id="selectById" resultType="com.shopjoy.ecadminapi.base.ec.pm.data.dto.PmCacheDto">
        SELECT
            c.*,
            ste.site_nm AS site_nm,
            cd_ct.code_label AS cache_type_cd_nm
        FROM pm_cache c
            LEFT JOIN sy_site ste ON ste.site_id = c.site_id
            LEFT JOIN sy_code cd_ct ON cd_ct.code_grp = 'CACHE_TYPE' AND cd_ct.code_value = c.cache_type_cd
        WHERE c.cache_id = #{id}
    </select>

    <!-- 캐시(충전금) 목록 조회 -->
    <select id="selectList" resultType="com.shopjoy.ecadminapi.base.ec.pm.data.dto.PmCacheDto">
        SELECT
            c.*,
            ste.site_nm AS site_nm,
            cd_ct.code_label AS cache_type_cd_nm
        FROM pm_cache c
            LEFT JOIN sy_site ste ON ste.site_id = c.site_id
            LEFT JOIN sy_code cd_ct ON cd_ct.code_grp = 'CACHE_TYPE' AND cd_ct.code_value = c.cache_type_cd
            <include refid="pmCacheCond"/>
        <choose>
            <when test="p.sort == 'id_asc'">ORDER BY c.cache_id ASC</when>
            <when test="p.sort == 'id_desc'">ORDER BY c.cache_id DESC</when>
            <when test="p.sort == 'nm_asc'">ORDER BY c.member_nm ASC</when>
            <when test="p.sort == 'nm_desc'">ORDER BY c.member_nm DESC</when>
            <when test="p.sort == 'reg_asc'">ORDER BY c.reg_date ASC</when>
            <when test="p.sort == 'reg_desc'">ORDER BY c.reg_date DESC</when>
            <otherwise>ORDER BY c.reg_date DESC</otherwise>
        </choose>
        <if test="p.limit != null and p.offset != null">LIMIT #{p.limit} OFFSET #{p.offset}</if>
    </select>

    <!-- 캐시(충전금) 페이징 목록 조회 -->
    <select id="selectPageList" resultType="com.shopjoy.ecadminapi.base.ec.pm.data.dto.PmCacheDto">
        SELECT
            c.*,
            ste.site_nm AS site_nm,
            cd_ct.code_label AS cache_type_cd_nm
        FROM pm_cache c
            LEFT JOIN sy_site ste ON ste.site_id = c.site_id
            LEFT JOIN sy_code cd_ct ON cd_ct.code_grp = 'CACHE_TYPE' AND cd_ct.code_value = c.cache_type_cd
            <include refid="pmCacheCond"/>
        <choose>
            <when test="p.sort == 'id_asc'">ORDER BY c.cache_id ASC</when>
            <when test="p.sort == 'id_desc'">ORDER BY c.cache_id DESC</when>
            <when test="p.sort == 'nm_asc'">ORDER BY c.member_nm ASC</when>
            <when test="p.sort == 'nm_desc'">ORDER BY c.member_nm DESC</when>
            <when test="p.sort == 'reg_asc'">ORDER BY c.reg_date ASC</when>
            <when test="p.sort == 'reg_desc'">ORDER BY c.reg_date DESC</when>
            <otherwise>ORDER BY c.reg_date DESC</otherwise>
        </choose>
        LIMIT #{p.limit} OFFSET #{p.offset}
    </select>

    <!-- 캐시(충전금) 전체 건수 조회 -->
    <select id="selectPageCount" resultType="long">
        SELECT COUNT(*)
        FROM pm_cache c
            <include refid="pmCacheCond"/>
    </select>

    <!-- 캐시(충전금) 선택 필드 수정 -->
    <update id="updateSelective" parameterType="com.shopjoy.ecadminapi.base.ec.pm.data.entity.PmCache">
        UPDATE pm_cache
        <set>
            <if test="siteId != null">site_id = #{siteId},</if>
            <if test="memberId != null">member_id = #{memberId},</if>
            <if test="memberNm != null">member_nm = #{memberNm},</if>
            <if test="cacheTypeCd != null">cache_type_cd = #{cacheTypeCd},</if>
            <if test="cacheAmt != null">cache_amt = #{cacheAmt},</if>
            <if test="balanceAmt != null">balance_amt = #{balanceAmt},</if>
            <if test="refId != null">ref_id = #{refId},</if>
            <if test="cacheDesc != null">cache_desc = #{cacheDesc},</if>
            <if test="procUserId != null">proc_user_id = #{procUserId},</if>
            <if test="cacheDate != null">cache_date = #{cacheDate},</if>
            <if test="expireDate != null">expire_date = #{expireDate},</if>
            <if test="updBy != null">upd_by = #{updBy},</if>
            <if test="updDate != null">upd_date = #{updDate},</if>
        </set>
        WHERE cache_id = #{cacheId}
    </update>

</mapper>
