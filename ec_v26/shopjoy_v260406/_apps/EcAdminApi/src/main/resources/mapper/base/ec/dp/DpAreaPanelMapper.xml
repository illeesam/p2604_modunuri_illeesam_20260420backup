<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shopjoy.ecadminapi.base.ec.dp.mapper.DpAreaPanelMapper">

    <!-- 전시 영역-패널 매핑 조회 조건 -->
    <sql id="dpAreaPanelCond">
        <where>
            <if test="p.dateStart != null and p.dateStart != ''">AND p.reg_date &gt;= #{p.dateStart}::DATE</if>
            <if test="p.dateEnd   != null and p.dateEnd   != ''">AND p.reg_date &lt;  (#{p.dateEnd}::DATE + INTERVAL '1 day')</if>
        </where>
    </sql>

    <!-- 전시 영역-패널 매핑 상세 조회 -->
    <select id="selectById" resultType="com.shopjoy.ecadminapi.base.ec.dp.data.dto.DpAreaPanelDto">
        SELECT
            p.*,
            are.area_nm AS area_nm,
            pan.panel_nm AS panel_nm
        FROM dp_area_panel p
            LEFT JOIN dp_area are ON are.area_id = p.area_id
            LEFT JOIN dp_panel pan ON pan.panel_id = p.panel_id
        WHERE p.area_panel_id = #{id}
    </select>

    <!-- 전시 영역-패널 매핑 목록 조회 -->
    <select id="selectList" resultType="com.shopjoy.ecadminapi.base.ec.dp.data.dto.DpAreaPanelDto">
        SELECT
            p.*,
            are.area_nm AS area_nm,
            pan.panel_nm AS panel_nm
        FROM dp_area_panel p
            LEFT JOIN dp_area are ON are.area_id = p.area_id
            LEFT JOIN dp_panel pan ON pan.panel_id = p.panel_id
            <include refid="dpAreaPanelCond"/>
        <choose>
            <when test="p.sort == 'id_asc'">ORDER BY p.area_panel_id ASC</when>
            <when test="p.sort == 'id_desc'">ORDER BY p.area_panel_id DESC</when>
            <when test="p.sort == 'reg_asc'">ORDER BY p.reg_date ASC</when>
            <when test="p.sort == 'reg_desc'">ORDER BY p.reg_date DESC</when>
            <otherwise>ORDER BY p.reg_date DESC</otherwise>
        </choose>
        <if test="p.limit != null and p.offset != null">LIMIT #{p.limit} OFFSET #{p.offset}</if>
    </select>

    <!-- 전시 영역-패널 매핑 페이징 목록 조회 -->
    <select id="selectPageList" resultType="com.shopjoy.ecadminapi.base.ec.dp.data.dto.DpAreaPanelDto">
        SELECT
            p.*,
            are.area_nm AS area_nm,
            pan.panel_nm AS panel_nm
        FROM dp_area_panel p
            LEFT JOIN dp_area are ON are.area_id = p.area_id
            LEFT JOIN dp_panel pan ON pan.panel_id = p.panel_id
            <include refid="dpAreaPanelCond"/>
        <choose>
            <when test="p.sort == 'id_asc'">ORDER BY p.area_panel_id ASC</when>
            <when test="p.sort == 'id_desc'">ORDER BY p.area_panel_id DESC</when>
            <when test="p.sort == 'reg_asc'">ORDER BY p.reg_date ASC</when>
            <when test="p.sort == 'reg_desc'">ORDER BY p.reg_date DESC</when>
            <otherwise>ORDER BY p.reg_date DESC</otherwise>
        </choose>
        LIMIT #{p.limit} OFFSET #{p.offset}
    </select>

    <!-- 전시 영역-패널 매핑 전체 건수 조회 -->
    <select id="selectPageCount" resultType="long">
        SELECT COUNT(*)
        FROM dp_area_panel p
            <include refid="dpAreaPanelCond"/>
    </select>

    <!-- 전시 영역-패널 매핑 선택 필드 수정 -->
    <update id="updateSelective" parameterType="com.shopjoy.ecadminapi.base.ec.dp.data.entity.DpAreaPanel">
        UPDATE dp_area_panel
        <set>
            <if test="areaId != null">area_id = #{areaId},</if>
            <if test="panelId != null">panel_id = #{panelId},</if>
            <if test="panelSortOrd != null">panel_sort_ord = #{panelSortOrd},</if>
            <if test="visibilityTargets != null">visibility_targets = #{visibilityTargets},</if>
            <if test="dispYn != null">disp_yn = #{dispYn},</if>
            <if test="dispStartDate != null">disp_start_date = #{dispStartDate},</if>
            <if test="dispStartTime != null">disp_start_time = #{dispStartTime},</if>
            <if test="dispEndDate != null">disp_end_date = #{dispEndDate},</if>
            <if test="dispEndTime != null">disp_end_time = #{dispEndTime},</if>
            <if test="dispEnv != null">disp_env = #{dispEnv},</if>
            <if test="useYn != null">use_yn = #{useYn},</if>
            <if test="updBy != null">upd_by = #{updBy},</if>
            <if test="updDate != null">upd_date = #{updDate},</if>
        </set>
        WHERE area_panel_id = #{areaPanelId}
    </update>

</mapper>
