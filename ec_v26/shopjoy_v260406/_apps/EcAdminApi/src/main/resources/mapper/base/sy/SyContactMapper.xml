<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shopjoy.ecadminapi.base.sy.mapper.SyContactMapper">

    <!-- 문의 조회 조건 -->
    <sql id="syContactCond">
        <where>
            <if test="p.siteId != null and p.siteId != ''">AND c.site_id = #{p.siteId}</if>
            <if test="p.status != null and p.status != ''">AND c.contact_status_cd = #{p.status}</if>
            <if test="p.kw != null and p.kw != ''">
                AND (c.member_nm ILIKE '%' || #{p.kw} || '%'
                  OR c.contact_title ILIKE '%' || #{p.kw} || '%')
            </if>
            <if test="p.dateStart != null and p.dateStart != ''">AND c.reg_date &gt;= #{p.dateStart}::DATE</if>
            <if test="p.dateEnd   != null and p.dateEnd   != ''">AND c.reg_date &lt;  (#{p.dateEnd}::DATE + INTERVAL '1 day')</if>
        </where>
    </sql>

    <!-- 문의 상세 조회 -->
    <select id="selectById" resultType="com.shopjoy.ecadminapi.base.sy.data.dto.SyContactDto">
        SELECT
            c.*,
            ste.site_nm AS site_nm,
            cd_cs.code_label AS contact_status_cd_nm
        FROM sy_contact c
            LEFT JOIN sy_site ste ON ste.site_id = c.site_id
            LEFT JOIN sy_code cd_cs ON cd_cs.code_grp = 'CONTACT_STATUS' AND cd_cs.code_value = c.contact_status_cd
        WHERE c.contact_id = #{id}
    </select>

    <!-- 문의 목록 조회 -->
    <select id="selectList" resultType="com.shopjoy.ecadminapi.base.sy.data.dto.SyContactDto">
        SELECT
            c.*,
            ste.site_nm AS site_nm,
            cd_cs.code_label AS contact_status_cd_nm
        FROM sy_contact c
            LEFT JOIN sy_site ste ON ste.site_id = c.site_id
            LEFT JOIN sy_code cd_cs ON cd_cs.code_grp = 'CONTACT_STATUS' AND cd_cs.code_value = c.contact_status_cd
            <include refid="syContactCond"/>
        <choose>
            <when test="p.sort == 'id_asc'">ORDER BY c.contact_id ASC</when>
            <when test="p.sort == 'id_desc'">ORDER BY c.contact_id DESC</when>
            <when test="p.sort == 'nm_asc'">ORDER BY c.member_nm ASC</when>
            <when test="p.sort == 'nm_desc'">ORDER BY c.member_nm DESC</when>
            <when test="p.sort == 'reg_asc'">ORDER BY c.reg_date ASC</when>
            <when test="p.sort == 'reg_desc'">ORDER BY c.reg_date DESC</when>
            <otherwise>ORDER BY c.reg_date DESC</otherwise>
        </choose>
        <if test="p.limit != null and p.offset != null">LIMIT #{p.limit} OFFSET #{p.offset}</if>
    </select>

    <!-- 문의 페이징 목록 조회 -->
    <select id="selectPageList" resultType="com.shopjoy.ecadminapi.base.sy.data.dto.SyContactDto">
        SELECT
            c.*,
            ste.site_nm AS site_nm,
            cd_cs.code_label AS contact_status_cd_nm
        FROM sy_contact c
            LEFT JOIN sy_site ste ON ste.site_id = c.site_id
            LEFT JOIN sy_code cd_cs ON cd_cs.code_grp = 'CONTACT_STATUS' AND cd_cs.code_value = c.contact_status_cd
            <include refid="syContactCond"/>
        <choose>
            <when test="p.sort == 'id_asc'">ORDER BY c.contact_id ASC</when>
            <when test="p.sort == 'id_desc'">ORDER BY c.contact_id DESC</when>
            <when test="p.sort == 'nm_asc'">ORDER BY c.member_nm ASC</when>
            <when test="p.sort == 'nm_desc'">ORDER BY c.member_nm DESC</when>
            <when test="p.sort == 'reg_asc'">ORDER BY c.reg_date ASC</when>
            <when test="p.sort == 'reg_desc'">ORDER BY c.reg_date DESC</when>
            <otherwise>ORDER BY c.reg_date DESC</otherwise>
        </choose>
        LIMIT #{p.limit} OFFSET #{p.offset}
    </select>

    <!-- 문의 전체 건수 조회 -->
    <select id="selectPageCount" resultType="long">
        SELECT COUNT(*)
        FROM sy_contact c
            <include refid="syContactCond"/>
    </select>

    <!-- 문의 선택 필드 수정 -->
    <update id="updateSelective" parameterType="com.shopjoy.ecadminapi.base.sy.data.entity.SyContact">
        UPDATE sy_contact
        <set>
            <if test="siteId != null">site_id = #{siteId},</if>
            <if test="memberId != null">member_id = #{memberId},</if>
            <if test="memberNm != null">member_nm = #{memberNm},</if>
            <if test="categoryCd != null">category_cd = #{categoryCd},</if>
            <if test="contactTitle != null">contact_title = #{contactTitle},</if>
            <if test="contactContent != null">contact_content = #{contactContent},</if>
            <if test="attachGrpId != null">attach_grp_id = #{attachGrpId},</if>
            <if test="contactStatusCd != null">contact_status_cd = #{contactStatusCd},</if>
            <if test="contactAnswer != null">contact_answer = #{contactAnswer},</if>
            <if test="answerUserId != null">answer_user_id = #{answerUserId},</if>
            <if test="answerDate != null">answer_date = #{answerDate},</if>
            <if test="contactDate != null">contact_date = #{contactDate},</if>
            <if test="updBy != null">upd_by = #{updBy},</if>
            <if test="updDate != null">upd_date = #{updDate},</if>
        </set>
        WHERE contact_id = #{contactId}
    </update>

</mapper>
