<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shopjoy.ecadminapi.base.ec.pm.mapper.PmSaveIssueMapper">

    <!-- 적립금 지급 이력 조회 조건 -->
    <sql id="pmSaveIssueCond">
        <where>
            <if test="p.siteId != null and p.siteId != ''">AND i.site_id = #{p.siteId}</if>
            <if test="p.status != null and p.status != ''">AND i.issue_status_cd = #{p.status}</if>
            <if test="p.typeCd != null and p.typeCd != ''">AND i.save_issue_type_cd = #{p.typeCd}</if>
            <if test="p.dateStart != null and p.dateStart != ''">AND i.reg_date &gt;= #{p.dateStart}::DATE</if>
            <if test="p.dateEnd   != null and p.dateEnd   != ''">AND i.reg_date &lt;  (#{p.dateEnd}::DATE + INTERVAL '1 day')</if>
        </where>
    </sql>

    <!-- 적립금 지급 이력 상세 조회 -->
    <select id="selectById" resultType="com.shopjoy.ecadminapi.base.ec.pm.data.dto.PmSaveIssueDto">
        SELECT
            i.*,
            ste.site_nm AS site_nm,
            mem.member_nm AS member_nm,
            ord.member_nm AS order_nm,
            ite.prod_nm AS order_item_nm,
            prd.prod_nm AS prod_nm,
            cd_sit.code_label AS save_issue_type_cd_nm,
            cd_sis.code_label AS issue_status_cd_nm
        FROM pm_save_issue i
            LEFT JOIN sy_site ste ON ste.site_id = i.site_id
            LEFT JOIN mb_member mem ON mem.member_id = i.member_id
            LEFT JOIN od_order ord ON ord.order_id = i.order_id
            LEFT JOIN od_order_item ite ON ite.order_item_id = i.order_item_id
            LEFT JOIN pd_prod prd ON prd.prod_id = i.prod_id
            LEFT JOIN sy_code cd_sit ON cd_sit.code_grp = 'SAVE_ISSUE_TYPE' AND cd_sit.code_value = i.save_issue_type_cd
            LEFT JOIN sy_code cd_sis ON cd_sis.code_grp = 'SAVE_ISSUE_STATUS' AND cd_sis.code_value = i.issue_status_cd
        WHERE i.save_issue_id = #{id}
    </select>

    <!-- 적립금 지급 이력 목록 조회 -->
    <select id="selectList" resultType="com.shopjoy.ecadminapi.base.ec.pm.data.dto.PmSaveIssueDto">
        SELECT
            i.*,
            ste.site_nm AS site_nm,
            mem.member_nm AS member_nm,
            ord.member_nm AS order_nm,
            ite.prod_nm AS order_item_nm,
            prd.prod_nm AS prod_nm,
            cd_sit.code_label AS save_issue_type_cd_nm,
            cd_sis.code_label AS issue_status_cd_nm
        FROM pm_save_issue i
            LEFT JOIN sy_site ste ON ste.site_id = i.site_id
            LEFT JOIN mb_member mem ON mem.member_id = i.member_id
            LEFT JOIN od_order ord ON ord.order_id = i.order_id
            LEFT JOIN od_order_item ite ON ite.order_item_id = i.order_item_id
            LEFT JOIN pd_prod prd ON prd.prod_id = i.prod_id
            LEFT JOIN sy_code cd_sit ON cd_sit.code_grp = 'SAVE_ISSUE_TYPE' AND cd_sit.code_value = i.save_issue_type_cd
            LEFT JOIN sy_code cd_sis ON cd_sis.code_grp = 'SAVE_ISSUE_STATUS' AND cd_sis.code_value = i.issue_status_cd
            <include refid="pmSaveIssueCond"/>
        <choose>
            <when test="p.sort == 'id_asc'">ORDER BY i.save_issue_id ASC</when>
            <when test="p.sort == 'id_desc'">ORDER BY i.save_issue_id DESC</when>
            <when test="p.sort == 'reg_asc'">ORDER BY i.reg_date ASC</when>
            <when test="p.sort == 'reg_desc'">ORDER BY i.reg_date DESC</when>
            <otherwise>ORDER BY i.reg_date DESC</otherwise>
        </choose>
        <if test="p.limit != null and p.offset != null">LIMIT #{p.limit} OFFSET #{p.offset}</if>
    </select>

    <!-- 적립금 지급 이력 페이징 목록 조회 -->
    <select id="selectPageList" resultType="com.shopjoy.ecadminapi.base.ec.pm.data.dto.PmSaveIssueDto">
        SELECT
            i.*,
            ste.site_nm AS site_nm,
            mem.member_nm AS member_nm,
            ord.member_nm AS order_nm,
            ite.prod_nm AS order_item_nm,
            prd.prod_nm AS prod_nm,
            cd_sit.code_label AS save_issue_type_cd_nm,
            cd_sis.code_label AS issue_status_cd_nm
        FROM pm_save_issue i
            LEFT JOIN sy_site ste ON ste.site_id = i.site_id
            LEFT JOIN mb_member mem ON mem.member_id = i.member_id
            LEFT JOIN od_order ord ON ord.order_id = i.order_id
            LEFT JOIN od_order_item ite ON ite.order_item_id = i.order_item_id
            LEFT JOIN pd_prod prd ON prd.prod_id = i.prod_id
            LEFT JOIN sy_code cd_sit ON cd_sit.code_grp = 'SAVE_ISSUE_TYPE' AND cd_sit.code_value = i.save_issue_type_cd
            LEFT JOIN sy_code cd_sis ON cd_sis.code_grp = 'SAVE_ISSUE_STATUS' AND cd_sis.code_value = i.issue_status_cd
            <include refid="pmSaveIssueCond"/>
        <choose>
            <when test="p.sort == 'id_asc'">ORDER BY i.save_issue_id ASC</when>
            <when test="p.sort == 'id_desc'">ORDER BY i.save_issue_id DESC</when>
            <when test="p.sort == 'reg_asc'">ORDER BY i.reg_date ASC</when>
            <when test="p.sort == 'reg_desc'">ORDER BY i.reg_date DESC</when>
            <otherwise>ORDER BY i.reg_date DESC</otherwise>
        </choose>
        LIMIT #{p.limit} OFFSET #{p.offset}
    </select>

    <!-- 적립금 지급 이력 전체 건수 조회 -->
    <select id="selectPageCount" resultType="long">
        SELECT COUNT(*)
        FROM pm_save_issue i
            <include refid="pmSaveIssueCond"/>
    </select>

    <!-- 적립금 지급 이력 선택 필드 수정 -->
    <update id="updateSelective" parameterType="com.shopjoy.ecadminapi.base.ec.pm.data.entity.PmSaveIssue">
        UPDATE pm_save_issue
        <set>
            <if test="siteId != null">site_id = #{siteId},</if>
            <if test="memberId != null">member_id = #{memberId},</if>
            <if test="saveIssueTypeCd != null">save_issue_type_cd = #{saveIssueTypeCd},</if>
            <if test="saveAmt != null">save_amt = #{saveAmt},</if>
            <if test="saveRate != null">save_rate = #{saveRate},</if>
            <if test="refTypeCd != null">ref_type_cd = #{refTypeCd},</if>
            <if test="refId != null">ref_id = #{refId},</if>
            <if test="orderId != null">order_id = #{orderId},</if>
            <if test="orderItemId != null">order_item_id = #{orderItemId},</if>
            <if test="prodId != null">prod_id = #{prodId},</if>
            <if test="expireDate != null">expire_date = #{expireDate},</if>
            <if test="issueStatusCd != null">issue_status_cd = #{issueStatusCd},</if>
            <if test="issueStatusCdBefore != null">issue_status_cd_before = #{issueStatusCdBefore},</if>
            <if test="saveMemo != null">save_memo = #{saveMemo},</if>
            <if test="updBy != null">upd_by = #{updBy},</if>
            <if test="updDate != null">upd_date = #{updDate},</if>
        </set>
        WHERE save_issue_id = #{saveIssueId}
    </update>

</mapper>
