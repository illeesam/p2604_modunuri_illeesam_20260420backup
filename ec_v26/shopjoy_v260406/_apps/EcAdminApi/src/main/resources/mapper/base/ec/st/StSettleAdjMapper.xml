<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shopjoy.ecadminapi.base.ec.st.mapper.StSettleAdjMapper">

    <!-- 정산 조정 조회 조건 -->
    <sql id="stSettleAdjCond">
        <where>
            <if test="p.siteId != null and p.siteId != ''">AND a.site_id = #{p.siteId}</if>
            <if test="p.typeCd != null and p.typeCd != ''">AND a.adj_type_cd = #{p.typeCd}</if>
            <if test="p.dateStart != null and p.dateStart != ''">AND a.reg_date &gt;= #{p.dateStart}::DATE</if>
            <if test="p.dateEnd   != null and p.dateEnd   != ''">AND a.reg_date &lt;  (#{p.dateEnd}::DATE + INTERVAL '1 day')</if>
        </where>
    </sql>

    <!-- 정산 조정 상세 조회 -->
    <select id="selectById" resultType="com.shopjoy.ecadminapi.base.ec.st.data.dto.StSettleAdjDto">
        SELECT
            a.*,
            ste.site_nm AS site_nm,
            cd_sat.code_label AS adj_type_cd_nm
        FROM st_settle_adj a
            LEFT JOIN sy_site ste ON ste.site_id = a.site_id
            LEFT JOIN sy_code cd_sat ON cd_sat.code_grp = 'SETTLE_ADJ_TYPE' AND cd_sat.code_value = a.adj_type_cd
        WHERE a.settle_adj_id = #{id}
    </select>

    <!-- 정산 조정 목록 조회 -->
    <select id="selectList" resultType="com.shopjoy.ecadminapi.base.ec.st.data.dto.StSettleAdjDto">
        SELECT
            a.*,
            ste.site_nm AS site_nm,
            cd_sat.code_label AS adj_type_cd_nm
        FROM st_settle_adj a
            LEFT JOIN sy_site ste ON ste.site_id = a.site_id
            LEFT JOIN sy_code cd_sat ON cd_sat.code_grp = 'SETTLE_ADJ_TYPE' AND cd_sat.code_value = a.adj_type_cd
            <include refid="stSettleAdjCond"/>
        <choose>
            <when test="p.sort == 'id_asc'">ORDER BY a.settle_adj_id ASC</when>
            <when test="p.sort == 'id_desc'">ORDER BY a.settle_adj_id DESC</when>
            <when test="p.sort == 'reg_asc'">ORDER BY a.reg_date ASC</when>
            <when test="p.sort == 'reg_desc'">ORDER BY a.reg_date DESC</when>
            <otherwise>ORDER BY a.reg_date DESC</otherwise>
        </choose>
        <if test="p.limit != null and p.offset != null">LIMIT #{p.limit} OFFSET #{p.offset}</if>
    </select>

    <!-- 정산 조정 페이징 목록 조회 -->
    <select id="selectPageList" resultType="com.shopjoy.ecadminapi.base.ec.st.data.dto.StSettleAdjDto">
        SELECT
            a.*,
            ste.site_nm AS site_nm,
            cd_sat.code_label AS adj_type_cd_nm
        FROM st_settle_adj a
            LEFT JOIN sy_site ste ON ste.site_id = a.site_id
            LEFT JOIN sy_code cd_sat ON cd_sat.code_grp = 'SETTLE_ADJ_TYPE' AND cd_sat.code_value = a.adj_type_cd
            <include refid="stSettleAdjCond"/>
        <choose>
            <when test="p.sort == 'id_asc'">ORDER BY a.settle_adj_id ASC</when>
            <when test="p.sort == 'id_desc'">ORDER BY a.settle_adj_id DESC</when>
            <when test="p.sort == 'reg_asc'">ORDER BY a.reg_date ASC</when>
            <when test="p.sort == 'reg_desc'">ORDER BY a.reg_date DESC</when>
            <otherwise>ORDER BY a.reg_date DESC</otherwise>
        </choose>
        LIMIT #{p.limit} OFFSET #{p.offset}
    </select>

    <!-- 정산 조정 전체 건수 조회 -->
    <select id="selectPageCount" resultType="long">
        SELECT COUNT(*)
        FROM st_settle_adj a
            <include refid="stSettleAdjCond"/>
    </select>

    <!-- 정산 조정 선택 필드 수정 -->
    <update id="updateSelective" parameterType="com.shopjoy.ecadminapi.base.ec.st.data.entity.StSettleAdj">
        UPDATE st_settle_adj
        <set>
            <if test="settleId != null">settle_id = #{settleId},</if>
            <if test="siteId != null">site_id = #{siteId},</if>
            <if test="adjTypeCd != null">adj_type_cd = #{adjTypeCd},</if>
            <if test="adjAmt != null">adj_amt = #{adjAmt},</if>
            <if test="adjReason != null">adj_reason = #{adjReason},</if>
            <if test="settleAdjMemo != null">settle_adj_memo = #{settleAdjMemo},</if>
            <if test="updBy != null">upd_by = #{updBy},</if>
            <if test="updDate != null">upd_date = #{updDate},</if>
        </set>
        WHERE settle_adj_id = #{settleAdjId}
    </update>

</mapper>
