<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shopjoy.ecadminapi.base.ec.pm.mapper.PmEventItemMapper">

    <!-- 이벤트 대상 상품 조회 조건 -->
    <sql id="pmEventItemCond">
        <where>
            <if test="p.siteId != null and p.siteId != ''">AND i.site_id = #{p.siteId}</if>
            <if test="p.typeCd != null and p.typeCd != ''">AND i.target_type_cd = #{p.typeCd}</if>
            <if test="p.dateStart != null and p.dateStart != ''">AND i.reg_date &gt;= #{p.dateStart}::DATE</if>
            <if test="p.dateEnd   != null and p.dateEnd   != ''">AND i.reg_date &lt;  (#{p.dateEnd}::DATE + INTERVAL '1 day')</if>
        </where>
    </sql>

    <!-- 이벤트 대상 상품 상세 조회 -->
    <select id="selectById" resultType="com.shopjoy.ecadminapi.base.ec.pm.data.dto.PmEventItemDto">
        SELECT
            i.*,
            evt.event_nm AS event_nm,
            ste.site_nm AS site_nm,
            cd_eit.code_label AS target_type_cd_nm
        FROM pm_event_item i
            LEFT JOIN pm_event evt ON evt.event_id = i.event_id
            LEFT JOIN sy_site ste ON ste.site_id = i.site_id
            LEFT JOIN sy_code cd_eit ON cd_eit.code_grp = 'EVENT_ITEM_TARGET' AND cd_eit.code_value = i.target_type_cd
        WHERE i.event_item_id = #{id}
    </select>

    <!-- 이벤트 대상 상품 목록 조회 -->
    <select id="selectList" resultType="com.shopjoy.ecadminapi.base.ec.pm.data.dto.PmEventItemDto">
        SELECT
            i.*,
            evt.event_nm AS event_nm,
            ste.site_nm AS site_nm,
            cd_eit.code_label AS target_type_cd_nm
        FROM pm_event_item i
            LEFT JOIN pm_event evt ON evt.event_id = i.event_id
            LEFT JOIN sy_site ste ON ste.site_id = i.site_id
            LEFT JOIN sy_code cd_eit ON cd_eit.code_grp = 'EVENT_ITEM_TARGET' AND cd_eit.code_value = i.target_type_cd
            <include refid="pmEventItemCond"/>
        <choose>
            <when test="p.sort == 'id_asc'">ORDER BY i.event_item_id ASC</when>
            <when test="p.sort == 'id_desc'">ORDER BY i.event_item_id DESC</when>
            <when test="p.sort == 'reg_asc'">ORDER BY i.reg_date ASC</when>
            <when test="p.sort == 'reg_desc'">ORDER BY i.reg_date DESC</when>
            <otherwise>ORDER BY i.reg_date DESC</otherwise>
        </choose>
        <if test="p.limit != null and p.offset != null">LIMIT #{p.limit} OFFSET #{p.offset}</if>
    </select>

    <!-- 이벤트 대상 상품 페이징 목록 조회 -->
    <select id="selectPageList" resultType="com.shopjoy.ecadminapi.base.ec.pm.data.dto.PmEventItemDto">
        SELECT
            i.*,
            evt.event_nm AS event_nm,
            ste.site_nm AS site_nm,
            cd_eit.code_label AS target_type_cd_nm
        FROM pm_event_item i
            LEFT JOIN pm_event evt ON evt.event_id = i.event_id
            LEFT JOIN sy_site ste ON ste.site_id = i.site_id
            LEFT JOIN sy_code cd_eit ON cd_eit.code_grp = 'EVENT_ITEM_TARGET' AND cd_eit.code_value = i.target_type_cd
            <include refid="pmEventItemCond"/>
        <choose>
            <when test="p.sort == 'id_asc'">ORDER BY i.event_item_id ASC</when>
            <when test="p.sort == 'id_desc'">ORDER BY i.event_item_id DESC</when>
            <when test="p.sort == 'reg_asc'">ORDER BY i.reg_date ASC</when>
            <when test="p.sort == 'reg_desc'">ORDER BY i.reg_date DESC</when>
            <otherwise>ORDER BY i.reg_date DESC</otherwise>
        </choose>
        LIMIT #{p.limit} OFFSET #{p.offset}
    </select>

    <!-- 이벤트 대상 상품 전체 건수 조회 -->
    <select id="selectPageCount" resultType="long">
        SELECT COUNT(*)
        FROM pm_event_item i
            <include refid="pmEventItemCond"/>
    </select>

    <!-- 이벤트 대상 상품 선택 필드 수정 -->
    <update id="updateSelective" parameterType="com.shopjoy.ecadminapi.base.ec.pm.data.entity.PmEventItem">
        UPDATE pm_event_item
        <set>
            <if test="eventId != null">event_id = #{eventId},</if>
            <if test="siteId != null">site_id = #{siteId},</if>
            <if test="targetTypeCd != null">target_type_cd = #{targetTypeCd},</if>
            <if test="targetId != null">target_id = #{targetId},</if>
            <if test="sortNo != null">sort_no = #{sortNo},</if>
        </set>
        WHERE event_item_id = #{eventItemId}
    </update>

</mapper>
