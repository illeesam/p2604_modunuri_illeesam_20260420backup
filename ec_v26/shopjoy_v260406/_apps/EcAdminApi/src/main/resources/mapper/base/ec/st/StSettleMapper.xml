<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shopjoy.ecadminapi.base.ec.st.mapper.StSettleMapper">

    <!-- 정산 조회 조건 -->
    <sql id="stSettleCond">
        <where>
            <if test="p.siteId != null and p.siteId != ''">AND s.site_id = #{p.siteId}</if>
            <if test="p.vendorId != null and p.vendorId != ''">AND s.vendor_id = #{p.vendorId}</if>
            <if test="p.status != null and p.status != ''">AND s.settle_status_cd = #{p.status}</if>
            <if test="p.dateStart != null and p.dateStart != ''">AND s.reg_date &gt;= #{p.dateStart}::DATE</if>
            <if test="p.dateEnd   != null and p.dateEnd   != ''">AND s.reg_date &lt;  (#{p.dateEnd}::DATE + INTERVAL '1 day')</if>
        </where>
    </sql>

    <!-- 정산 상세 조회 -->
    <select id="selectById" resultType="com.shopjoy.ecadminapi.base.ec.st.data.dto.StSettleDto">
        SELECT
            s.*,
            vnd.vendor_nm AS vendor_nm,
            ste.site_nm AS site_nm,
            cd_ss.code_label AS settle_status_cd_nm
        FROM st_settle s
            LEFT JOIN sy_vendor vnd ON vnd.vendor_id = s.vendor_id
            LEFT JOIN sy_site ste ON ste.site_id = s.site_id
            LEFT JOIN sy_code cd_ss ON cd_ss.code_grp = 'SETTLE_STATUS' AND cd_ss.code_value = s.settle_status_cd
        WHERE s.settle_id = #{id}
    </select>

    <!-- 정산 목록 조회 -->
    <select id="selectList" resultType="com.shopjoy.ecadminapi.base.ec.st.data.dto.StSettleDto">
        SELECT
            s.*,
            vnd.vendor_nm AS vendor_nm,
            ste.site_nm AS site_nm,
            cd_ss.code_label AS settle_status_cd_nm
        FROM st_settle s
            LEFT JOIN sy_vendor vnd ON vnd.vendor_id = s.vendor_id
            LEFT JOIN sy_site ste ON ste.site_id = s.site_id
            LEFT JOIN sy_code cd_ss ON cd_ss.code_grp = 'SETTLE_STATUS' AND cd_ss.code_value = s.settle_status_cd
            <include refid="stSettleCond"/>
        <choose>
            <when test="p.sort == 'id_asc'">ORDER BY s.settle_id ASC</when>
            <when test="p.sort == 'id_desc'">ORDER BY s.settle_id DESC</when>
            <when test="p.sort == 'reg_asc'">ORDER BY s.settle_ym ASC</when>
            <when test="p.sort == 'reg_desc'">ORDER BY s.settle_ym DESC</when>
            <otherwise>ORDER BY s.reg_date DESC</otherwise>
        </choose>
        <if test="p.limit != null and p.offset != null">LIMIT #{p.limit} OFFSET #{p.offset}</if>
    </select>

    <!-- 정산 페이징 목록 조회 -->
    <select id="selectPageList" resultType="com.shopjoy.ecadminapi.base.ec.st.data.dto.StSettleDto">
        SELECT
            s.*,
            vnd.vendor_nm AS vendor_nm,
            ste.site_nm AS site_nm,
            cd_ss.code_label AS settle_status_cd_nm
        FROM st_settle s
            LEFT JOIN sy_vendor vnd ON vnd.vendor_id = s.vendor_id
            LEFT JOIN sy_site ste ON ste.site_id = s.site_id
            LEFT JOIN sy_code cd_ss ON cd_ss.code_grp = 'SETTLE_STATUS' AND cd_ss.code_value = s.settle_status_cd
            <include refid="stSettleCond"/>
        <choose>
            <when test="p.sort == 'id_asc'">ORDER BY s.settle_id ASC</when>
            <when test="p.sort == 'id_desc'">ORDER BY s.settle_id DESC</when>
            <when test="p.sort == 'reg_asc'">ORDER BY s.settle_ym ASC</when>
            <when test="p.sort == 'reg_desc'">ORDER BY s.settle_ym DESC</when>
            <otherwise>ORDER BY s.reg_date DESC</otherwise>
        </choose>
        LIMIT #{p.limit} OFFSET #{p.offset}
    </select>

    <!-- 정산 전체 건수 조회 -->
    <select id="selectPageCount" resultType="long">
        SELECT COUNT(*)
        FROM st_settle s
            <include refid="stSettleCond"/>
    </select>

    <!-- 정산 선택 필드 수정 -->
    <update id="updateSelective" parameterType="com.shopjoy.ecadminapi.base.ec.st.data.entity.StSettle">
        UPDATE st_settle
        <set>
            <if test="siteId != null">site_id = #{siteId},</if>
            <if test="vendorId != null">vendor_id = #{vendorId},</if>
            <if test="settleYm != null">settle_ym = #{settleYm},</if>
            <if test="settleStartDate != null">settle_start_date = #{settleStartDate},</if>
            <if test="settleEndDate != null">settle_end_date = #{settleEndDate},</if>
            <if test="totalOrderAmt != null">total_order_amt = #{totalOrderAmt},</if>
            <if test="totalReturnAmt != null">total_return_amt = #{totalReturnAmt},</if>
            <if test="totalClaimCnt != null">total_claim_cnt = #{totalClaimCnt},</if>
            <if test="totalDiscntAmt != null">total_discnt_amt = #{totalDiscntAmt},</if>
            <if test="commissionRate != null">commission_rate = #{commissionRate},</if>
            <if test="commissionAmt != null">commission_amt = #{commissionAmt},</if>
            <if test="settleAmt != null">settle_amt = #{settleAmt},</if>
            <if test="adjAmt != null">adj_amt = #{adjAmt},</if>
            <if test="etcAdjAmt != null">etc_adj_amt = #{etcAdjAmt},</if>
            <if test="finalSettleAmt != null">final_settle_amt = #{finalSettleAmt},</if>
            <if test="settleStatusCd != null">settle_status_cd = #{settleStatusCd},</if>
            <if test="settleStatusCdBefore != null">settle_status_cd_before = #{settleStatusCdBefore},</if>
            <if test="settleMemo != null">settle_memo = #{settleMemo},</if>
            <if test="updBy != null">upd_by = #{updBy},</if>
            <if test="updDate != null">upd_date = #{updDate},</if>
        </set>
        WHERE settle_id = #{settleId}
    </update>

</mapper>
